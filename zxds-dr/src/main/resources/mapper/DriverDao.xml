<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.zxds.dr.db.dao.DriverDao">

    <select id="hasDriver" resultType="java.lang.Long" parameterType="map">
        select count(id) as ct
        from tb_driver
        <where>
            <if test="driverId != null and openId = ''">
                and driver_id = #{dirverId}
            </if>
            <if test="openId != null">
                and open_id = #{openId}
            </if>
        </where>
    </select>

    <insert id="registerNewDriver" parameterType="Map">
        INSERT INTO tb_driver
        SET id = #{driverId},
            open_id = #{openId},
            nickname = #{nickname},
            photo = #{photo},
            real_auth = 1,
            summary = '{"level":0,"totalOrder":0,"weekOrder":0,"weekComment":0,"appeal":0}',
            archive = false,
            `status` = 1
    </insert>
</mapper>
